name: pending_button
description: "Button peding is an animated button with asynchrnonise state (Idle, Pending, Error, Success)"
version: 0.0.8
homepage: https://github.com/MoezAyadiDev/pending_button
repository: https://github.com/MoezAyadiDev/pending_button
screenshots:
  - description: "Screenshot for different type of button pending"
    path: screenshots/pending_image.png
topics:
  - button
  - loading
  - buttonstate

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
